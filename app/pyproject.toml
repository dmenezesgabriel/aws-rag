[project]
name = "aws-rag"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = "~=3.12"
dependencies = [
    "aws-lambda-powertools>=3.22.0",
    "aws-xray-sdk>=2.15.0",
]

